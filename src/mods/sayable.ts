import { sayable } from 'wechaty-puppet/payloads'
import { Sayable as type } from 'wechaty-puppet/types'

export const {
  attatchment,
  audio,
  contact,
  emoticon,
  image,
  location,
  miniProgram,
  text,
  url,
  video,
  post,
}                   = sayable

export {
  type,
}
